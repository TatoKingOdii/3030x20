import { FutureDateDirective } from './future-date.directive';

describe('FutureDateDirective', () => {
  it('should create an instance', () => {
    const directive = new FutureDateDirective();
    expect(directive).toBeTruthy();
  });
});
